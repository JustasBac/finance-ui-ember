<Modals::-TriggerButton>
  <p
    class="text-2xl text-center font-bold btn btn-ghost whitespace-nowrap"
    {{on 'click' (fn (mut this.isModalOpen) true)}}
  >
    {{if this.currentMonthValue this.currentMonthValue '-'}}
    {{this.currency}}
    <Tooltip @side="bottom">
      Click to edit
    </Tooltip>
  </p>
</Modals::-TriggerButton>
<Modals::-base @isOpen={{this.isModalOpen}}>
  <:header>
    <div>
      {{capitalize @type}}
      {{concat '(' this.currency ')'}}
      <div class="font-normal">
        {{this.currentMonth}}
      </div>
    </div>
  </:header>
  <:body>
    <InputField
      class="mr-2"
      @type="number"
      @value={{this.editedCurrentMonthValue}}
      @onInput={{fn (mut this.editedCurrentMonthValue)}}
      @placeholder="Enter your {{@type}} for {{this.currentMonth}}"
    />
    <div class="flex justify-between items-center mt-5">
      <Modals::-TriggerButton>
        <span class="btn" {{on 'click' (fn (mut this.isModalOpen) false)}}>
          Close
        </span>
      </Modals::-TriggerButton>
      <Button {{on 'click' this.saveChanges}}>
        <FaIcon @icon="floppy-disk" class="mr-1" />
        Save
      </Button>
    </div>
  </:body>
</Modals::-base>