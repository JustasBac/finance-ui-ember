<BasicDropdown
  @onOpen={{this.onDropdownOpen}}
  @onClose={{this.onDropdownClose}} as |dd|
>
  <dd.Trigger>
    <label
      tabindex="0"
      class="rounded cursor-pointer font-light text-bas btn btn-ghost hover:bg-slate-400/40 text-lg"
    >
      {{this.currencyService.selectedCurrency.symbol}}
      <Tooltip>
        <span class="font-bold">
          Selected currency:
        </span>
        <div>
          {{this.currencyService.selectedCurrency.name}}
          <span class="text-xs">
            {{concat '(' this.currencyService.selectedCurrency.code ')'}}
          </span>
        </div>
      </Tooltip>
    </label>
  </dd.Trigger>
  <dd.Content>
    <div class="shadow-xl card-body w-[70rem]">
      <CurrencySelection::Dashboard />
      <div class="card-actions justify-end">
        <Button class="btn-sm" {{on 'click' dd.actions.close}}>
          Close
        </Button>
      </div>
    </div>
  </dd.Content>
</BasicDropdown>