<div class="alert {{this.type}} shadow-lg max-w-[25rem]">
  <div class="flex w-full">
    {{#if (eq @type 'info')}}
      <FaIcon @icon="info-circle" class="text-xl" />
    {{else}}
      <FaIcon @icon="triangle-exclamation" class="text-xl" />
    {{/if}}
    <span class="w-full text-center">
      {{@text}}
    </span>
  </div>
</div>