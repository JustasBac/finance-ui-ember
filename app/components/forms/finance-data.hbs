<InputField
  class="mt-3"
  @type="number"
  @value={{@data.income}}
  @min={{1}}
  @label="Income ({{@data.currencySymbol}})"
  @onInput={{fn (mut @data.income)}}
  @placeholder="Enter income"
/>
{{log 'incoming' @data}}
<InputField
  class="mt-3"
  @type="number"
  @value={{@data.spendings}}
  @min={{1}}
  @label="Spendings ({{@data.currencySymbol}})"
  @onInput={{fn (mut @data.spendings)}}
  @placeholder="Enter spendings"
/>
<InputField
  class="mt-3"
  @type="number"
  @value={{@data.totalBalance}}
  @min={{1}}
  @label="Total balance ({{@data.currencySymbol}})"
  @onInput={{fn (mut @data.totalBalance)}}
  @placeholder="Enter total balance"
/>