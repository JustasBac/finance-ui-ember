<div class="form-control w-full" ...attributes>
  {{#if @label}}
    <label class="label py-1.5">
      <span class="label-text leading-none dark:text-gray-400">
        {{@label}}
        {{#if @tooltipText}}
          <span class="ml-1">
            <FaIcon @icon="circle-info" />
            <Tooltip>
              {{@tooltipText}}
            </Tooltip>
          </span>
        {{/if}}
      </span>
    </label>
  {{/if}}
  <input
    value={{@value}}
    type={{this.type}}
    min={{@min}}
    placeholder={{@placeholder}}
    class="input input-bordered w-full text-sm dark:bg-gray-700 dark:text-gray-200
      {{if @size (concat 'input-' @size)}}

      {{unless this.isFormValidated (unless @exceptFromValidation 'input-error')
      }}"
    {{on 'input' this.updateInputValue}}
  />
</div>