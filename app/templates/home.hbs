{{page-title 'Home'}}
{{#if this.existingSavingPlans}}
  saving plans
{{else}}
  <div class="flex flex-col items-center justify-center">
    {{!-- {{#if this.isSavingPlanCreationBlockOpen}} --}}
    <div class="w-96 flex flex-col">
      <h3 class="card-title mb-3">
        Create a new saving plan
      </h3>
      <Input
        @value={{this.savingGoalTitle}}
        @label="Goal name"
        @onInput={{fn (mut this.savingGoalTitle)}}
        @placeholder="Enter goal name"
      />
      <div class="flex">
        <Input
          class="mt-3"
          @type="number"
          @value={{this.goalAmount}}
          @label="Goal amount"
          @onInput={{fn (mut this.goalAmount)}}
          @placeholder="Enter goal amount"
        />
        <Input
          class="mt-3 basis-1/3 ml-2"
          @value={{this.enteredCurrency}}
          @onInput={{fn (mut this.enteredCurrency)}}
          @label="Currency"
          @placeholder="€"
        />
      </div>
      <DatePicker
        @date={{this.selectedDate}}
        @onDateChange={{fn (mut this.selectedDate)}}
      />
      <div class="flex justify-between mt-5">
        <Button
          class="mt-3 btn-ghost bg-gray-300"
          {{on 'click' (fn (mut this.isSavingPlanCreationBlockOpen) false)}}
        >
          <FaIcon @icon="arrow-left" class="mr-1" />
          Go back
        </Button>
        <Button class="mt-3" {{on 'click' this.createSavingPlan}}>
          <FaIcon @icon="floppy-disk" class="mr-1" />
          Save
        </Button>
      </div>
    </div>
    {{!-- {{else}}
      <Alert @text="You have no saving plans" />
      <Button
        class="mt-3"
        {{on 'click' (fn (mut this.isSavingPlanCreationBlockOpen) true)}}
      >
        <FaIcon @icon="plus" class="mr-1" />
        Create a new saving plan
      </Button>
    {{/if}} --}}
  </div>
{{/if}}